server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tropsmart/TS_ms_config-data.git
          searchPaths: config-data
          default-label: "dev"