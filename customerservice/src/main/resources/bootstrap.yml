server:
  port: 8092

spring:
  application:
    name: customer-service
  cloud:
    config:
      uri: http://localhost:8081
      username: root
      password: s3cr3t
  boot:
    admin:
      client:
        url: http://localhost:8086

springdoc:
  api-docs:
    path: /ts-api-docs
  swagger-ui:
    path: /index.html